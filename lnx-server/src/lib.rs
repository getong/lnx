use std::net::SocketAddr;
use std::path::PathBuf;
use anyhow::Result;

/// Runs the lnx server.
pub async fn run(
    listen_address: SocketAddr,
    data_path: PathBuf,
) -> Result<()> {
    
    Ok(())
}

